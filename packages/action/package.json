{
  "name": "@git-contrib-viz/action",
  "version": "1.0.0",
  "description": "GitHub Action entry point — fetches contributions and writes the animated SVG",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@git-contrib-viz/generator": "*",
    "@git-contrib-viz/theme": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
