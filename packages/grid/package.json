{
  "name": "@git-contrib-viz/grid",
  "version": "1.0.0",
  "description": "Builds the collectible grid and computes a traversal path through active cells",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "@git-contrib-viz/github-contributions": "*",
    "@git-contrib-viz/theme": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
