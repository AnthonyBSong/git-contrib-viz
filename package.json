{
  "name": "git-pacman",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace packages/github-contributions && npm run build --workspace packages/theme && npm run build --workspace packages/grid && npm run build --workspace packages/svg-creator && npm run build --workspace packages/generator && npm run build --workspace packages/cli && npm run build --workspace packages/action",
    "bundle": "npm run build && ncc build packages/action/dist/index.js -o dist --license licenses.txt",
    "cli": "npm run build && node packages/cli/dist/cli.js",
    "test": "npm run build && node scripts/test.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.0.0"
  }
}
